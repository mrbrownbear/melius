name: Localize Melius

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The site is already localized. No automatic live-site crawl is enabled."
